services:
  # Main API service
  api:
    build:
      context: .
    ports:
      - "8001:8001"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - ./data:/app/data
    restart: unless-stopped

volumes:
  data:
    driver: local